// Re-export the shared Prisma client from @github-pr-code-smell-detector/core.
// Kept as a shim so existing `@/lib/prisma` imports keep working.
export { prisma } from "@github-pr-code-smell-detector/core";
